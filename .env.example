# Server
PORT=3001

# Database
# Supports absolute paths and "~/" home-directory shorthand on Linux.
DB_PATH=~/mcp-memory/memories.db

# Optional: override the built frontend directory served by Express in production.
CLIENT_DIST=./client/dist
